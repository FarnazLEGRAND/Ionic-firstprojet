import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Projet01-Ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
